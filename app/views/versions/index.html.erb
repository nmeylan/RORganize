<%#
# Author: Nicolas Meylan
# Date: 15 août 2012
# Encoding: UTF-8
# File: versions.html.erb
%>

<div class="contextual">
  <h1><%= t(:label_setting_plural) %> - <%= t(:link_versions) %></h1>
  <div class="splitcontentright">
    <%= link_to t(:link_new_version), {:action => 'new', :project_id => @project.identifier}, {:class => 'icon icon-add'} if current_user.allowed_to?('new','Versions',@project) %>
  </div>
</div>
<%= render :partial => 'versions/list' %>
<%= render :partial => 'settings/sidebar' %>