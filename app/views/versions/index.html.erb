<%#
# Author: Nicolas Meylan
# Date: 15 août 2012
# Encoding: UTF-8
# File: versions.html.erb
%>

<%= contextual @versions_decorator.collection_contextual_title("#{t(:label_setting_plural)} - #{t(:link_versions)}") do %>
    <%= @versions_decorator.new_link %>
<% end %>
<%= render partial: 'versions/list', locals: {versions_decorator: @versions_decorator} %>
<%= render partial: 'settings/sidebar', locals: {project: @project} %>