<%#
# Author: Nicolas Meylan
# Date: 16 août 2012
# Encoding: UTF-8
# File: version_edit.html.erb
%>

<%= contextual "#{t(:label_setting_plural)} - #{t(:link_versions)}" %>

<%= form_for @version, url: version_path, method: :put, html: {class: 'form'} do |f| %>
    <%= render partial: 'versions/form', locals: {f: f, version: @version} %>
    <%= submit_tag t(:button_save) %>
<% end %>
<%= render partial: 'settings/sidebar', locals: {project: @project} %>