<%#
# Author: Nicolas Meylan
# Date: 6 avr. 2013
# Encoding: UTF-8
# File: index.html.erb
%>

<%= contextual @documents_decorator.filtered_collection_contextual_title(t(:label_documents)) do %>
    <%= @documents_decorator.new_link %>
<% end %>
<%= filter_tag('document', Document.filtered_attributes, project_documents_path(@project.slug, query_id: params[:query_id]), true, {user: User.current, project: @project, filter_content: session["#{@project.slug}_controller_document_filter"], type: 'Document'}) %>

<%= @documents_decorator.display_collection %>


<ul id="documents-toolbox" class="jeegoocontext box toolbox"></ul>
<%= render partial: 'shared/delete_modal', locals: {collection_deletion: nil} %>
<%= render partial: 'queries/create_overlay', locals: {query: @query, project: @project} %>
<%= render partial: 'documents/sidebar', locals: {custom_queries_decorator: @custom_queries_decorator, project: @project, selected_query: @query} %>
