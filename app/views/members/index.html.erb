<%#
# Author: Nicolas Meylan
# Date: 30 sept. 2012
# Encoding: UTF-8
# File: index.html.erb
%>
<%= contextual @members_decorator.collection_contextual_title("#{t(:label_setting_plural)} - #{t(:link_members)}") do %>
    <%= @members_decorator.new_link %>
<% end %>

<%= render partial: 'settings/sidebar', locals: {project: @project} %>
<%= @members_decorator.display_collection %>
<%= render partial: 'members/add_overlay', locals: {roles: @members_decorator.context[:role], users: users} %>

