<%#
# Author: Nicolas Meylan
# Date: 23 oct. 2012
# Encoding: UTF-8
# File: edit.html.erb
%>

<div class="contextual">
  <h1><%= t(:link_edit_role)%></h1>
</div>

<%= form_for @tracker, :url => tracker_path, :method => :put, :html => {:class => 'form'} do |f|%>
  <%= render :partial => 'trackers/form', :locals => {:f => f} %>
  <%= submit_tag t(:button_save) %>
<% end %>
