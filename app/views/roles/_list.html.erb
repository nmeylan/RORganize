<%#
# Author: Nicolas Meylan
# Date: 23 oct. 2012
# Encoding: UTF-8
# File: _list.html.erb
%>

<div id="roles_content">
  <table class="role list">
    <tr class="header">
      <th>Name</th>
      <th></th>
    </tr>
    <% @roles.each do |role| %>
      <tr class="odd_even" id="<%= role.id %>">
        <td class="list_left name"><% if current_user.allowed_to?('edit','Roles') %>
            <%= link_to role.name, edit_role_path(role.id)%>
          <% else %>
            <%= role.name %>
          <% end %>
        </td>
        <td class="action">
          <%= link_to_with_permissions(glyph(t(:link_delete), 'trashcan'), role_path(role.id), nil, {:method => :delete, :remote => true, :confirm => t(:text_delete_item ) }) %>
      </tr>
    <%end%>
  </table>
</div>
