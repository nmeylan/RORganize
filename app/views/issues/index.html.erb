<%#
# Author: Nicolas Meylan
# Date: 8 juil. 2012
# Encoding: UTF-8
# File: index.html.erb
%>
<%= contextual t(:label_issue_plural) do %>
    <%= @issues.new_link %>
<% end %>

<%= filter_tag('issue', Issue.filtered_attributes, issues_path(@project.slug), true, {user: current_user, project: @project, filter_content: session["#{@project.slug}_controller_issues_filter"]}) %>
  <%= render :partial => 'issues/list' %>
  <%= render :partial => 'issues/sidebar' %>


