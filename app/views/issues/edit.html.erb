<%#
# Author: Nicolas Meylan
# Date: 18 juil. 2012
# Encoding: UTF-8
# File: edit.html.erb
%>
<%= contextual t(:label_edit_issue) %>
<%= form_for(@issue_decorator, :url => issue_path, :method => :put, :html => {:class => 'form',:multipart => true}) do |f|%>
  <%= render :partial => 'issues/form', :locals => {:f => f, :form_content => form_content} %>
  <%= submit_tag t(:button_save) %>
<% end %>