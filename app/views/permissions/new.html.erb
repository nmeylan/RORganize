<%#
# Author: Nicolas Meylan
# Date: 13 oct. 2012
# Encoding: UTF-8
# File: new.html.erb
%>

<%= contextual t(:link_new_permission) %>

<%= form_for @permission_decorator, :url => {:controller => 'permissions'}, :html => {:class => 'form'} do |f| %>
    <%= render :partial => 'permissions/form', :locals => {:f => f, :controllers => controllers, permission_decorator: @permission_decorator} %>
    <%= submit_tag t(:button_create) %>
<% end %>