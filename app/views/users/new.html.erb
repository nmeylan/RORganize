<%#
# Author: Nicolas Meylan
# Date: 29 sept. 2012
# Encoding: UTF-8
# File: new.html.erb
%>

<%= contextual t(:link_new_user) %>

<%= form_for @user, :url => {:controller => 'users'}, :html => {:class => 'form'} do |f| %>
    <%= render :partial => 'users/form', :locals => {:f => f, user: @user} %>
    <%= submit_tag t(:button_create) %>
<% end %>