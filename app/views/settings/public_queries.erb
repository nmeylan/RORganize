<%#
# Author: Nicolas Meylan
# Date: 6 févr. 2013
# Encoding: UTF-8
# File: custom_queries.erb
%>
<%= render partial: 'settings/sidebar', locals: {project: @project} %>

<%= contextual @queries_decorator.collection_contextual_title t(:link_custom_queries) %>

<%= render partial: 'queries/list', locals: {queries_decorator: @queries_decorator} %>